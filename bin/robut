#!/usr/bin/env ruby
require 'rubygems'
begin
  require 'robut'
rescue LoadError
  require 'bundler/setup'
  require 'robut'
end
require 'ostruct'
require 'logger'

load ARGV[0] || './Chatfile'

connection = Robut::Connection.new.connect
loop do
  connection.join_all_pending_rooms
  sleep 1
end
